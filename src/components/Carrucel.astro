---
import ambientes from "@/assets/banners/ambientes.png";
import equipos from "@/assets/banners/equipos.png";
import estudios from "@/assets/banners/estudios.png";
import pediatricos from "@/assets/banners/pediatricos.png";
import ImageCarrucel from "./ImageCarrucel.astro";
import Ambientes from "@/assets/icons/ambientes.astro";
import Pediatrico from "@/assets/icons/pediatrico.astro";
import Equipos from "@/assets/icons/equipos.astro";
import Hours from "@/assets/icons/24h.astro";
const agregatedvalues = [
  {
    texto: "Ambientes acondicionados",
    imagen: ambientes,
    position: "right",
    icon: Ambientes,
  },
  {
    texto: "Equipos de ultima generación",
    imagen: equipos,
    position: "left",
    icon: Equipos,
  },
  {
    texto: "Estudios las 24 horas del día",
    imagen: estudios,
    position: "center",
    icon: Hours,
  },
  {
    texto: "Estudios pedriátricos (niños recién nacidos)",
    imagen: pediatricos,
    position: "center",
    icon: Pediatrico,
  },
];
---

<div class="carousel-container">
  <div class="carousel">
    {
      agregatedvalues.map((img, index) => (
        <div class="carousel-slide" data-index={index}>
          <ImageCarrucel
            Arte={img.imagen}
            title={img.texto}
            titlePosition="left"
            icon={img.icon}
          />
        </div>
      ))
    }
  </div>
  <button class="carousel-button prev" aria-label="Anterior">&#10094;</button>
  <button class="carousel-button next" aria-label="Siguiente">&#10095;</button>
</div>

<style>
  .carousel-container {
    position: relative;
    max-width: 800px;
    margin: 0 auto;
    overflow: hidden;
  }

  .carousel {
    display: flex;
    transition: transform 0.5s ease-in-out;
  }

  .carousel-slide {
    min-width: 100%;
    position: relative;
  }

  .carousel-slide img {
    width: 100%;
    height: auto;
    object-fit: cover;
  }

  .carousel-button {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background: rgba(0, 0, 0, 0.5);
    color: white;
    padding: 1rem;
    border: none;
    cursor: pointer;
    border-radius: 50%;
  }

  .prev {
    left: 1rem;
  }

  .next {
    right: 1rem;
  }
</style>
<script>
  function setupCarousel() {
    const carousel = document.querySelector(".carousel") as HTMLElement;
    const slides = document.querySelectorAll(".carousel-slide");
    const prevButton = document.querySelector(".prev");
    const nextButton = document.querySelector(".next");

    if (!carousel || !prevButton || !nextButton) {
      console.error("No se encontraron los elementos necesarios del carrusel");
      return;
    }

    let currentSlide = 0;

    function updateCarousel() {
      carousel.style.transform = `translateX(-${currentSlide * 100}%)`;
    }

    prevButton.addEventListener("click", () => {
      currentSlide = (currentSlide - 1 + slides.length) % slides.length;
      updateCarousel();
    });

    nextButton.addEventListener("click", () => {
      currentSlide = (currentSlide + 1) % slides.length;
      updateCarousel();
    });

    // Auto avance cada 5 segundos
    setInterval(() => {
      currentSlide = (currentSlide + 1) % slides.length;
      updateCarousel();
    }, 3000);
  }

  // Ejecutar cuando el DOM esté listo
  document.addEventListener("DOMContentLoaded", setupCarousel);
</script>
